/* Day 46
Question 136: Use console.log() to debug and track the value of a variable inside a loop. */
for (var i = 0; i < 10; i++) {
    console.log("The value of i in loop: ".concat(i)); // Logs the current iteration number and the value of i
}
